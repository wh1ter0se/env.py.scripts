# env.py.scripts

Scripts used for setting up and managing a Python development environmnet, using `uv` as a build tool.

This is intended to be a plug-and-play solution for setting up the environment on any system, and only requires that Python 3.9 (or newer) be installed.
